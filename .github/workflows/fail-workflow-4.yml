name: Fail Workflow 4

on:
  workflow_dispatch:

jobs:
  fail-job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step in Job 1
        run: |
          echo "This is Job 1"
          exit 1

  fail-job-2:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step in Job 2
        run: |
          echo "This is Job 2"
          exit 1
